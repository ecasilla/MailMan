<h1>Editing <%= @campaign.name %></h1>

<h3>Add Recipients</h3>
<%= form_for @campaign do |f| %>
  <%= f.collection_select(:recipient_ids, current_user.recipients, :id, :email, {@campaign_recipients}, multiple: true) %>
  <%= f.submit :save %>
<% end %>