<%= form_for @email_template, :url => campaign_email_template_path(@campaign) do |f| %>
  <%= f.label :Subject%>
  <%= f.text_field :name %>
  <%= f.label :body %>
  <%= f.text_area :body %>
  <%= f.submit "Submit" ,class: "button radius"  %>
<% end %>