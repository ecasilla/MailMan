<%= form_for @email_template, :url => user_campaign_email_template_path(@user, @campaign) do |f| %>
  <%= f.label :Template%>
  <%= f.text_field :name %>
  <%= f.label :body %>
  <%= f.text_area :body %>
  <%= f.submit "Submit" ,class: "button radius"  %>
<% end %>