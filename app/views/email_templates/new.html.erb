<%= link_to "Profile",user_path(@user) %>
<h3>Enter Your Email Body</h3>

<%= form_for @email_template do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :body %>
  <%= f.text_field :body %>
  <%= f.submit "Submit" ,class: "button radius"  %>
<% end %>
